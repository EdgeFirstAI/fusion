# GitHub Actions workflow for testing and code quality
#
# This workflow runs tests, performs static analysis, and collects coverage data
# for the EdgeFirst Fusion project. Coverage is reported to SonarCloud.
#
# Action Versions (hash-pinned, verified January 2026):
# - actions/checkout@0c366fd6a839edf440554fa01a7085ccba70ac98 (v4.2.2)
# - actions/cache@5a3ec84eff668545956fd18022155c47e93e2684 (v4.2.3)
# - actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f (v6.0.0)
# - actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 (v7.0.0)
# - dtolnay/rust-toolchain@stable
# - taiki-e/install-action@7e574ed8bb89811282a11aecb3fe1d043bf5bf0e (v2)
# - SonarSource/sonarqube-scan-action@a31c9398be7ace6bbfaf30c0bd5d415f843d45e9 (master)
#
# Runner Notes:
# - ubuntu-22.04: Standard GitHub-hosted runner (x86_64)
# - ubuntu-22.04-arm: GitHub-hosted ARM64 runner for native aarch64 builds/tests

name: Test

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

env:
  CARGO_TERM_COLOR: always

jobs:
  format:
    name: Format Check
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout code
        uses: actions/checkout@0c366fd6a839edf440554fa01a7085ccba70ac98  # v4.2.2

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - name: Check formatting
        run: cargo fmt --all --check

  # Matrixed across both architectures so the #[cfg(target_arch = "aarch64")]
  # NEON code -- which is what actually ships to the i.MX8MP -- gets linted.
  # An x86_64-only run compiles those blocks out entirely and sees nothing.
  # The two legs run concurrently, so this does not lengthen the pipeline.
  clippy:
    name: Lint with Clippy (${{ matrix.platform.name }})
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - name: x86_64
            runner: ubuntu-22.04
          - name: aarch64
            runner: ubuntu-22.04-arm
      fail-fast: false

    steps:
      - name: Checkout code
        uses: actions/checkout@0c366fd6a839edf440554fa01a7085ccba70ac98  # v4.2.2

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      # runner.os is "Linux" on both runners, so the platform name has to be in
      # the cache key or the two legs would clobber each other's target dir.
      - name: Cache cargo registry
        uses: actions/cache@5a3ec84eff668545956fd18022155c47e93e2684  # v4.2.3
        with:
          path: ~/.cargo/registry/index
          key: ${{ runner.os }}-${{ matrix.platform.name }}-cargo-index-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-${{ matrix.platform.name }}-cargo-index-

      - name: Cache cargo build
        uses: actions/cache@5a3ec84eff668545956fd18022155c47e93e2684  # v4.2.3
        with:
          path: target
          key: ${{ runner.os }}-${{ matrix.platform.name }}-cargo-clippy-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-${{ matrix.platform.name }}-cargo-clippy-

      - name: Run Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Generate Clippy report for SonarCloud
        run: |
          # Report generation only; the gate above is what fails the job.
          cargo clippy --workspace --all-targets --message-format=json 2>&1 \
            | tee clippy-report.json || true

      - name: Upload Clippy report
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f  # v6.0.0
        with:
          name: clippy-report-${{ matrix.platform.name }}
          path: clippy-report.json
          retention-days: 7

  test:
    name: Test and Coverage (${{ matrix.platform.name }})
    runs-on: ${{ matrix.platform.runner }}
    permissions:
      contents: read
      checks: write
      pull-requests: write
    strategy:
      matrix:
        platform:
          - name: x86_64
            runner: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
          - name: aarch64
            runner: ubuntu-22.04-arm
            target: aarch64-unknown-linux-gnu
      fail-fast: false

    steps:
      - name: Checkout code
        uses: actions/checkout@0c366fd6a839edf440554fa01a7085ccba70ac98  # v4.2.2
        with:
          fetch-depth: 0

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: llvm-tools-preview
          targets: ${{ matrix.platform.target }}

      - name: Install cargo-llvm-cov and cargo-nextest
        uses: taiki-e/install-action@7e574ed8bb89811282a11aecb3fe1d043bf5bf0e  # v2
        with:
          tool: cargo-llvm-cov,cargo-nextest

      - name: Cache cargo registry
        uses: actions/cache@5a3ec84eff668545956fd18022155c47e93e2684  # v4.2.3
        with:
          path: ~/.cargo/registry/index
          key: ${{ runner.os }}-${{ matrix.platform.name }}-cargo-index
          restore-keys: ${{ runner.os }}-${{ matrix.platform.name }}-cargo-index

      - name: Cache cargo build
        uses: actions/cache@5a3ec84eff668545956fd18022155c47e93e2684  # v4.2.3
        with:
          path: target
          key: ${{ runner.os }}-${{ matrix.platform.name }}-cargo-test
          restore-keys: ${{ runner.os }}-${{ matrix.platform.name }}-cargo-test

      - name: Run tests with coverage
        run: cargo llvm-cov nextest --workspace --lcov --output-path coverage.lcov

      # No doc test step: edgefirst-fusion is a binary-only crate, and rustdoc
      # only collects doctests from lib targets, so `cargo test --doc` exits
      # 101 with "no library targets found" rather than running anything. If a
      # lib target is ever added, reinstate the step -- without `|| true`.

      # Uploaded even when the test steps fail so a red job still leaves the
      # partial coverage report behind for diagnosis. The SonarCloud job gates
      # on needs.test.result == 'success', so it never consumes a partial run.
      - name: Upload coverage artifact
        if: always()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f  # v6.0.0
        with:
          name: coverage-${{ matrix.platform.name }}
          path: coverage.lcov
          retention-days: 30
          if-no-files-found: ignore

  # ==========================================================================
  # SonarCloud Analysis
  # ==========================================================================
  sonarcloud:
    name: SonarCloud Analysis
    runs-on: ubuntu-22.04
    needs: [clippy, test]
    if: |
      always() &&
      needs.test.result == 'success' &&
      (github.event_name == 'push' || github.event.pull_request.head.repo.full_name == github.repository)

    steps:
      - name: Checkout code
        uses: actions/checkout@0c366fd6a839edf440554fa01a7085ccba70ac98  # v4.2.2
        with:
          fetch-depth: 0

      - name: Download coverage artifacts
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131  # v7.0.0
        with:
          pattern: coverage-*
          path: coverage/

      - name: Download Clippy reports
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131  # v7.0.0
        with:
          pattern: clippy-report-*
          path: clippy/

      - name: Organize coverage files for SonarCloud
        run: |
          echo "=== Downloaded coverage artifacts ==="
          mkdir -p coverage/
          find coverage/ -type f

          # Collect all LCOV coverage reports
          COVERAGE_REPORTS=$(find coverage/ -name "*.lcov" | tr '\n' ',' | sed 's/,$//')

          echo "=== Coverage report paths ==="
          echo "Coverage reports: $COVERAGE_REPORTS"

          # Export for SonarCloud
          echo "COVERAGE_PATHS=$COVERAGE_REPORTS" >> $GITHUB_ENV

          echo ""
          echo "=== Clippy reports ==="
          find clippy/ -type f -name "*.json"

          # One report per architecture; Sonar accepts a comma-separated list
          CLIPPY_REPORTS=$(find clippy/ -name "clippy-report.json" | tr '\n' ',' | sed 's/,$//')

          echo "Clippy reports: $CLIPPY_REPORTS"
          echo "CLIPPY_PATHS=$CLIPPY_REPORTS" >> $GITHUB_ENV

      - name: SonarCloud Scan
        uses: SonarSource/sonarqube-scan-action@a31c9398be7ace6bbfaf30c0bd5d415f843d45e9  # master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.rust.lcov.reportPaths=${{ env.COVERAGE_PATHS }}
            -Dsonar.rust.clippy.reportPaths=${{ env.CLIPPY_PATHS }}

      - name: Generate coverage summary
        if: always()
        run: |
          echo "# EdgeFirst Fusion Coverage Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Test Platforms
          echo "## Test Platforms" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Platform | Architecture | Environment | Test Types |" >> $GITHUB_STEP_SUMMARY
          echo "|----------|--------------|-------------|------------|" >> $GITHUB_STEP_SUMMARY
          echo "| Ubuntu 22.04 | x86_64 | GitHub Runner | Unit tests |" >> $GITHUB_STEP_SUMMARY
          echo "| Ubuntu 22.04 ARM | aarch64 | GitHub ARM Runner | Unit tests |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Coverage Summary
          echo "## Coverage Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Parse coverage from LCOV files
          for coverage_file in $(find coverage/ -name "*.lcov" 2>/dev/null | sort); do
            if [ -f "$coverage_file" ]; then
              platform=$(basename "$(dirname "$coverage_file")" | sed 's/coverage-//')

              # Extract coverage metrics from LCOV (trim whitespace from wc output)
              lines_found=$(grep -c "^DA:" "$coverage_file" 2>/dev/null || echo 0)
              lines_hit=$(grep "^DA:" "$coverage_file" 2>/dev/null | grep -v ",0$" | wc -l | tr -d ' ')

              # Ensure numeric values
              lines_found=${lines_found:-0}
              lines_hit=${lines_hit:-0}

              if [ "$lines_found" -gt 0 ] 2>/dev/null; then
                line_pct=$(awk "BEGIN {printf \"%.1f\", ($lines_hit / $lines_found) * 100}")

                echo "**$platform**:" >> $GITHUB_STEP_SUMMARY
                echo "- Line coverage: ${line_pct}% (${lines_hit}/${lines_found} lines)" >> $GITHUB_STEP_SUMMARY
                echo "" >> $GITHUB_STEP_SUMMARY
              fi
            fi
          done

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Full analysis available on [SonarCloud](https://sonarcloud.io/dashboard?id=EdgeFirstAI_fusion)" >> $GITHUB_STEP_SUMMARY
